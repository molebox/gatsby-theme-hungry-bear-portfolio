import * as React from 'react';
import { Box } from './Box';
import { Title } from '../components/Title';

export default () => {
    return(
        <Box>
            <Title>Im the hero</Title>
        </Box>
    )
}
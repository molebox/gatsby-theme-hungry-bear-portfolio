import * as React from 'react';
import {Box} from './Box';
import { Title } from '../components/Title';

export default () => {
    return(
        <Box noBackground>
            <Title>Im the content!</Title>
        </Box>
    )
}